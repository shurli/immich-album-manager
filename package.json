{
  "name": "immich-album-manager",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Immich album manager with inline rename suggestions, merge, archive toggle, and bulk workflows",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "5.1.0"
  }
}
