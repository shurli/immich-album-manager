{
  "name": "immich-album-manager",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "No-pagination Immich album manager with multi-select merge workflow",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "5.1.0"
  }
}
